# term_conf
a term_conf is like confupanda asatiro raw3a insights to toggle the tab key moving focus. Alternatively, use esc then tab to move to the next interactive element on the page.
Attach them by dragging & dropping, selecting or passing them.
